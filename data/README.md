Place **`imdb_top_500.csv`** in this folder (`data/imdb_top_500.csv`) for the layout in the assignment brief.

`train.py` also accepts **`imdb_top_500.csv`** in the repository root if this folder has no CSV yet.
